- args:
    sql: CREATE TABLE "public"."users"("id" serial NOT NULL, "email" text NOT NULL,
      "password" text NOT NULL, "is_superuser" boolean NOT NULL DEFAULT false, "is_active"
      boolean NOT NULL DEFAULT false, "is_staff" boolean NOT NULL DEFAULT false, "first_name"
      text, "last_name" text, "description" text, PRIMARY KEY ("id") , UNIQUE ("id"),
      UNIQUE ("email"));
  type: run_sql
- args:
    name: users
    schema: public
  type: add_existing_table_or_view
