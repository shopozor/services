version: '3.6'
services:
  postgres:
    image: postgres:11.2-alpine
    restart: "no"
    volumes:
      - db_data:/var/lib/postgresql-test/data
  graphql-engine:
    image: hasura/graphql-engine:v1.0.0-beta.9
    ports:
      - 9000:8080
    depends_on:
      - postgres
    restart: "no"
    environment:
      HASURA_GRAPHQL_DATABASE_URL: postgres://postgres:@postgres:5432/postgres
      HASURA_GRAPHQL_ENABLE_CONSOLE: "true"
      HASURA_GRAPHQL_ENABLED_LOG_TYPES: startup, http-log, webhook-log, websocket-log, query-log
  hasura-service-tests:
    build:
      context: ./database-service
      dockerfile: ./Dockerfile
      target: unittest
    restart: "no"
    depends_on:
      - graphql-engine
    volumes:
      - ./database-service:/app
    entrypoint: pytest --hasura-endpoint http://graphql-engine:8080 -ra --junitxml=test-report.xml
  fixtures-service:
    build:
      context: ./database-service
      dockerfile: ./Dockerfile
      target: unittest
    restart: "no"
    environment:
      - PYTHONPATH=.:${PYTHONPATH}
    volumes:
      - .:/app
    entrypoint:
      - bash
      - -c
      - |
          ./database-service/tests/fixtures-generator/entrypoint.sh

volumes:
  db_data:
